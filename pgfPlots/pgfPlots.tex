\documentclass[class=letter,border=0pt]{standalone}
\usepackage{preamble}
\begin{document}
\pgfplotsset{
/pgfplots/legend image code/.code={
\draw[mark repeat=2,mark phase=2]
plot coordinates {
(0cm,0cm)
(0.1cm,0cm)        %% default is (0.3cm,0cm)
(0.2cm,0cm)         %% default is (0.6cm,0cm)
};%
}
}
	\begin{tikzpicture}
	\tikzset{>=latex}
	\begin{axis}[name=ax0,
	anchor=south east,
	font=\small,
        xlabel={$x$},
	%xticklabels={,,},
	%xtick={-0.6,-0.5,...,0.6},
        ylabel={$<C_f>$},
        %xmin=0.05, xmax=1,
        %ymin=-0.005, ymax=0.005,
        %xmode=log,
        %ymode=log,
	%y dir=reverse,
        %axis on top,
        xmajorgrids,
	minor tick num=1,
        ymajorgrids,
	xminorgrids,
	yminorgrids,
        legend style={at={(1.00,1.00)}, anchor=north east},
        legend entries={{SLE-4}},
	cycle list name = mynomarklist,
	mark repeat={1}]
		\addplot table [x={x}, y={Cf}] {8SLE_Cf.dat};
	\end{axis}
	\end{tikzpicture}
\end{document}
