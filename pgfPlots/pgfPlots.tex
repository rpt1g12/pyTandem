\documentclass[class=letter,border=0pt]{standalone}
\usepackage{preamble}
\begin{document}
\pgfplotsset{
/pgfplots/legend image code/.code={
\draw[mark repeat=2,mark phase=2]
plot coordinates {
(0cm,0cm)
(0.1cm,0cm)        %% default is (0.3cm,0cm)
(0.2cm,0cm)         %% default is (0.6cm,0cm)
};%
}
}
	\begin{tikzpicture}
	\tikzset{>=latex}
	\begin{axis}[
	%font=\small,
        xlabel={$St$},
	%xticklabels={,,},
        ylabel={$S_{C_{Df}C_{Df}}$},
        xmin=0.05, xmax=1,
        %ymin=0.2e-2, ymax=1.2e-2,
        xmode=log,
        ymode=log,
	%y dir=reverse,
        %axis on top,
        %width=\tikzw,
        %height=\tikzh,
	%x=2\textwidth,
	%y=1\textwidth,
	%xtick={-0.6,-0.5,...,0.6},
	grid=both,
        xmajorgrids,
	minor tick num=1,
        ymajorgrids,
	xminorgrids,
	yminorgrids,
        ymajorgrids,yminorgrids,
        legend style={at={(1.00,1.00)}, anchor=north east},
        legend entries={{SLE},{4WLE}},
	cycle list name = mynomarklist,
	mark repeat={10}]
		\addplot table [x={f}, y={SCd}]  {A00pClCdPSD.dat};
		\addplot table [x={f}, y={SCd}] {4A15pClCdPSD.dat};
	\end{axis}
	\end{tikzpicture}
\end{document}
